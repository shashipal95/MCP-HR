# HR Assistant MCP Chatbot – Python Dependencies
# Install: pip install -r requirements.txt

# ── MCP (Model Context Protocol) ─────────────────────────────────────────────
mcp>=1.0.0

# ── LLM (Groq) ───────────────────────────────────────────────────────────────
# Set GROQ_API_KEY in your environment before running.
# Get a free key at: https://console.groq.com
langchain-groq>=0.1.0

# ── Embeddings (HuggingFace — local, no API key needed) ──────────────────────
# Replaces OllamaEmbeddings. Downloads ~90 MB model on first run.
langchain-huggingface>=0.0.3
sentence-transformers>=2.0.0

# ── LangChain core ───────────────────────────────────────────────────────────
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langchain-core>=0.2.0

# ── Vector store ──────────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── Data ──────────────────────────────────────────────────────────────────────
pandas>=2.0.0
pypdf>=4.0.0

# ── UI & API ──────────────────────────────────────────────────────────────────
streamlit>=1.35.0
fastapi
uvicorn